@using X.PagedList
@using X.PagedList.Mvc.Core
@model IEnumerable<Gymany.Models.Order>

@{
    ViewData["Title"] = "Order";
    Layout = "GymOwnerLayout";
}


@* declare tag helper in areas *@
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<div class="page-wrapper">
    <div class="page-breadcrumb bg-white">
        <div class="row align-items-center">
            <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
                <h4 class="page-title">Order Managements</h4>
            </div>
        </div>
    </div>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <div class="white-box">
                    <h3 class="box-title">Order Managements</h3>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Order ID</th>
                                    <th>Customer ID</th>
                                    <th>Product ID</th>
                                    <th>Status</th>
                                    <th>Date</th>
                                    <th>Total</th>
                                    <th>Quantity</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var order in Model)
                                {
                                    <tr>
                                        <td>@order.order_id</td>
                                        <td>@order.cus_id</td>
                                        <td>@order.prod_id</td>
                
                                         @if (order.order_status.Equals("Success"))
                                        {
                                            <td class="text-success">@order.order_status</td>
                                        }
                                        else if (order.order_status.Equals("Waiting"))
                                        {
                                            <td class="text-warning">@order.order_status</td>
                                        }
                                        else
                                        {
                                            <td class="text-danger">@order.order_status</td>
                                        }
                                        <td>@order.order_startDate</td>
                                        <td>@order.order_totalPrice</td>
                                        <td>@order.order_quantity</td>
                                        <td>
                                            <a asp-controller="GymOwner" asp-action="UpdateOrder" class="btn btn-success"
                                                asp-route-id="@order.order_id" style="color: #fff;">Update Status Order</a>

                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        @Html.PagedListPager((IPagedList)Model, page => Url.Action("Order", new { page = page }),
                 new X.PagedList.Web.Common.PagedListRenderOptionsBase
        {
            LiElementClasses = new string[] { "page-item" },
            PageClasses = new string[] { "page-link" }
        })
    </div>
    <footer class="footer text-center"> 2024 © Gymany Admin by Group 05
    </footer>
</div>
</div>