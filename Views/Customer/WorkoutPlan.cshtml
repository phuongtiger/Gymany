
@model Gymany.Models.ListModels
@{
    ViewData["Title"] = "WorkoutPlan";
}


@* @if(@Model==null){
    <p>Bạn chưa là Member</p>
}else{
    @foreach (var item in Model)
    { *@
        <div class="container bg-black mt-4">
            <div class="row">
                <div class="col-md-12">
                    <div class="card bg-dark text-white mb-4">
                        <div class="card-header">
                           <input readonly class="form-control" id="inputName" type="hidden" placeholder="Enter your username" value=" @Html.DisplayFor(model => model.member.MemberID)">
                        </div>
                     
                    </div>
                </div>
            </div>
        </div>
    @* }
} *@
